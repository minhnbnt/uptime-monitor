{{- if .Values.traefik.enabled }}
{{- $ns := include "uptime-monitor.namespace" . -}}
{{- $httpPort := .Values.traefik.httpPort -}}
{{- range $name, $app := .Values.apps }}
{{- range $i, $path := $app.ingress }}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute

metadata:
  name: {{ $name }}-{{ $i }}
  namespace: {{ $ns }}
  labels:
    app.kubernetes.io/name: {{ $name }}

spec:
  entryPoints:
    - web
  routes:
    - match: PathPrefix(`{{ . }}`)
      kind: Rule
      services:
        - name: {{ $name }}
          port: {{ $app.port }}
      middlewares:
        - name: cors
{{- end }}
{{- end }}
{{- if .Values.gotrue.enabled }}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute

metadata:
  name: gotrue
  namespace: {{ $ns }}
  labels:
    app.kubernetes.io/name: gotrue

spec:
  entryPoints:
    - web
  routes:
    - match: PathPrefix(`/auth/v1`)
      kind: Rule
      services:
        - name: gotrue
          port: {{ .Values.gotrue.port }}
      middlewares:
        - name: cors
{{- end }}
{{- end }}
