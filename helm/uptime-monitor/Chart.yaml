apiVersion: v2
name: uptime-monitor
description: Uptime monitor microservices (auth, server, ping, ontime, importer, notification) with Traefik ingress
type: application
version: 0.1.0
appVersion: "1.0.0"
