apiVersion: v2
name: uptime-monitor
description: Uptime monitor microservices (auth, server, ping, ontime, importer, notification), exposed via the Kubernetes Gateway API (Cilium as the Gateway controller)
type: application
version: 0.1.0
appVersion: "1.0.0"
